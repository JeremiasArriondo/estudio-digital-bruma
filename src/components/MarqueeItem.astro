---
const { className = "" } = Astro.props;
---

<div
  class={`flex items-center justify-center px-4 py-2 rounded-lg bg-white dark:bg-slate-800 shadow-md border border-purple-200 dark:border-slate-700 transition-all hover:scale-105 hover:shadow-lg ${className}`}
>
  <slot />
</div>
